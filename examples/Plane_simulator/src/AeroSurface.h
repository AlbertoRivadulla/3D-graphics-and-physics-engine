#ifndef AERO_SURFACE_H
#define AERO_SURFACE_H

class AeroSurface {
    // TODO: Implement


    // TODO: Take this into account:
    // The z axis points forward (by default) in the different sections, so we need to check the relative velocity of the wind in that direction
    // The normal points in the local y direction 
};

#endif
